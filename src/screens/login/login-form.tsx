import {View, Text} from 'react-native';
import {StyledComponent} from 'nativewind';

export default function LoginForm() {
  // implement dimensions

  return (
    <StyledComponent component={View} tw="flex flex-col bg-white h-screen">
      <StyledComponent component={Text}>test</StyledComponent>
    </StyledComponent>
  );
}
